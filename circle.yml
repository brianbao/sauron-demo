general:
  branches:
    only:
      - master

test:
  override:
    - python test.py

notify:
  webhooks:
    - url: https://sauron-ui-elb-824878459.us-west-2.elb.amazonaws.com/circleci/webhook/

experimental:
  notify:
    branches:
      only:
        - master
